import React from 'react';

//Component to Render the page's Title
const Title = () => (
	<div>
		<h1 className="title-container__title">Full Stack Challenge</h1>
		<h3 className="title-container__subtitle">Enter a number to find median prime numbers...</h3>
	</div>
);


export default Title;